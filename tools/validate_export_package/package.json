{
  "name": "@resource-gallery/validate-export-package",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "validate-export-package": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "start": "pnpm build && node dist/cli.js",
    "test": "pnpm build"
  },
  "dependencies": {
    "@resource-gallery/export-schema": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
